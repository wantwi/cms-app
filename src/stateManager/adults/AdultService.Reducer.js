import { initialState as Initial_State } from '../initialState'

const AdultServiceReducer = (state, action = {}) => {
    const { type } = action
    switch (type) {
        default:
            return state
    }
}

export default AdultServiceReducer
