import { initialState as Initial_State } from '../initialState'

const ChildrenServiceReducers = (state, action = {}) => {
    const { type } = action
    switch (type) {
        default:
            return state
    }
}

export default ChildrenServiceReducers
